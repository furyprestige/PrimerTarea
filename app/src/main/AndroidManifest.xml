<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.alonzo.primertarea">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PrimerTarea">
        <activity
            android:name=".Settings"
            android:label="@string/title_activity_settings">

        </activity>
        <activity
            android:name=".InformacionContacto"
            android:screenOrientation="portrait"
            android:parentActivityName=".MisContactos"/>
        <activity
            android:name=".MisContactos"
            android:screenOrientation="portrait"></activity>
        <activity
            android:name=".ConfirmarDatos"
            android:screenOrientation="portrait" />
        <activity
            android:name=".FormularioContacto"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>